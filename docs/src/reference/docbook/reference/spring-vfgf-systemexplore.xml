<?xml version="1.0" encoding="UTF-8"?>
<chapter xmlns="http://docbook.org/ns/docbook" version="5.0"  xmlns:xlink="http://www.w3.org/1999/xlink" xmlns:xi="http://www.w3.org/2001/XInclude" xml:id="system-explorer">
  <title>System Explorer</title>
<section>
<para>The <emphasis role="bold">systemExplorer</emphasis> <a href="SystemExplorer.java">example</a> is a command-line application that allows the user to exercise GemFire's <code>com.gemstone.gemfire.admin</code> 
API. The example is located in the GemFire installation under 
<code>examples/dist/systemExplorer.</code></para>

<para>Before running this application, you need to configure your environment 
according to the instructions provided under the GemFire installation 
in <a href="../../EnvSetup.html">examples/EnvSetup.html</a>. </para>

<para>Once your environment is set, change to the <code>examples/dist/systemExplorer</code>
directory to run the application.</para>

<para>This sample startup line starts the application using all defaults:
<programlisting>
       java systemExplorer.SystemExplorer
</programlisting>
</para>

<para>You may optionally specify a <code>gemfire.properties</code> file as an argument to
<code>SystemExplorer</code>.  It will also automatically pick up any <code>gemfire.properties</code> 
file that you place in the <code>examples/dist/systemExplorer</code> directory.  The 
application will then use the values in the properties file to determine 
details on the system it will connect to:
<programlisting>
       java systemExplorer.SystemExplorer c:\gemfire.properties</programlisting>
</para>
<para>Another way to specify system details to the application includes the use
of <code>-Dpropertyname=value arguments</code>.  The following starts the application and
specifies connecting to a distributed system using multicast port 10412:
<programlisting>
       java -Dgemfire.mcast-port=10412 systemExplorer.SystemExplorer
</programlisting>

Full details on properties that can be used to specify connection details are
covered in the Javadocs for <code>com.gemstone.gemfire.distributed.DistributedSystem</code>.
</para>

<para>Once started, the application allows you to perform many of the 
administration activities available through the Java API. Enter <strong><code>help</code></strong> to see a list of commands.</para>

</section>
</chapter>

